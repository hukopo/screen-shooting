import "./styles.css";

export default function JobList() {
  const yyyy = [1, 2, 3];

  return (
    <div className="job-list">
      {yyyy.map((y) => (
        <div className="job">jjj</div>
      ))}
    </div>
  );
}
